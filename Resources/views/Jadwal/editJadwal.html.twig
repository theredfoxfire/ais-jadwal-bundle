{% block content %}
    <h1>Edit Dosen Form</h1>
    {%  if (form is not null) %}
    <form action="{{ url('api_1_post_update_dosen', {'id': dosen.id}) }}" {{ form_enctype(form) }} method="POST">
        {{ form_widget(form) }}
        <input type="submit" value="submit">
    </form>
    {% endif %}
{% endblock %}
