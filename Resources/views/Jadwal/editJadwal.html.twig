{% block content %}
    <h1>Edit Jadwal Form</h1>
    {%  if (form is not null) %}
    <form action="{{ url('api_1_post_update_jadwal', {'id': jadwal.id}) }}" {{ form_enctype(form) }} method="POST">
        {{ form_widget(form) }}
        <input type="submit" value="submit">
    </form>
    {% endif %}
{% endblock %}
