{% block content %}
    <h1>Create Jadwal Form</h1>
    {%  if (form is not null) %}
    <form action="{{ url('api_1_post_jadwal') }}" method="POST" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <input type="submit" value="submit">
    </form>
    {% endif %}
{% endblock %}
